# WALL ENCLOSURE FRAME AUDIT — Спринт-М

**Режим:** read-only reverse engineering. Production code не менялся, книги
открывались только на чтение, коммитов и пушей нет.

**Источник:** `Y:\Предварительные расчеты\Калькуляторы\Калькулятор ограждайки v1.5.xlsx`,
SHA-256 `4A9343A1E3149954DEC0F91D5398528F18016A8423EC204CE2B92A59F612DEAF`
(снят до и после каждого открытия, совпал).

**Объектные свидетельства:** ведомости 21876 (Каргалейка), 21604
(Кропоткин), 22258 (Хабаровск) и шесть копий калькулятора, сохранённых
расчётчиком по 21604, 21876, 22317.

**Монтажная инструкция:** «ГП по сборке неутеплённых Спринт-М»
(ООО «Гранд Проджект», Челябинск, 2019), раздел 8 «Монтаж стенового
ограждения» и раздел 9 «Обрамление проёмов». Получена после первой
редакции отчёта; подтверждает выводы, сделанные по формулам, и уточняет
два из них — см. раздел «Что добавила монтажная инструкция».

---

## 1. Схема стенового каркаса

```
          ┌──────────────────────────────────────┐  ← ВЕРХНИЙ ригель (TN)
          │                                      │     всегда ОДИНОЧНЫЙ профиль
          │   ▌        ▌        ▌        ▌       │
          ├───┼────────┼────────┼────────┼───────┤  ← ПРОМЕЖУТОЧНЫЙ ригель
          │   ▌        ▌        ▌        ▌       │     сечение по каталогу
          ├───┼────────┼────────┼────────┼───────┤     (] 1 шт, [] ][ [-] 2 шт)
          │   ▌        ▌        ▌        ▌       │
          ├───┼────────┼────────┼────────┼───────┤
          │   ▌        ▌        ▌        ▌       │
          └───┴────────┴────────┴────────┴───────┘  ← НИЖНИЙ ригель (TO)
          ↑   ↑                          ↑     ↑       всегда ОДИНОЧНЫЙ
       колонна  стойки ограждения      стойка  колонна
        рамы    (T, ≈2 м, в v1.5 ВЫКЛ)          рамы
          │←──────── шаг рам B13 ────────→│
```

Кронштейн (узловая сборка, столбец `AA`) ставится в каждом пересечении
ригеля со стойкой/колонной.

---

## 2. Полный список элементов

| Элемент | Тип | Где в книге | Профиль | Кол-во | Длина | Масса | Статус в v1.5 |
|---|---|---|---|---|---|---|---|
| Промежуточный ригель | горизонтальный | `TQ$4*$Z` | из каталога `V` | `TQ3 = ОКРВВЕРХ(B12/шаг)−1` на пролёт рам | шаг рам | `Z` (масса СЕЧЕНИЯ) | активен |
| Верхний ригель | горизонтальный | `$TN*B13`, подпись `TN6` | тот же профиль | 1 на пролёт рам | шаг рам | `Y` (масса ПРОФИЛЯ) | активен |
| Нижний ригель | горизонтальный | `$TO*B13`, подпись `TO6` | тот же профиль | 1 на пролёт рам | шаг рам | `Y` (масса ПРОФИЛЯ) | активен |
| Узловая сборка (кронштейн) | крепёж | `TQ$3*$AA` | — | `TQ3` на пролёт рам | — | `AA` = 1,5 / 0,75 кг | активен |
| Стойка ограждения | вертикальный | `$T`, таблица `A13:C24` | подбирается по высоте ригеля | `T4` на пролёт рам | высота стены `B12` | `T5 × вес/м` | **ВЫКЛЮЧЕН** |
| Стойка фахверка | вертикальный | в книге **отсутствует** | — | — | — | — | вне ограждайки |
| Обрамление проёмов | — | в книге **отсутствует** | — | — | — | — | вне ограждайки |

---

## 3. TASK 2 — горизонтальные направляющие

### Формула количества

```excel
TQ$3 = ОКРВВЕРХ.МАТ(Лист1!$B$12 / (шаг/1000)) − 1      ← промежуточные
Лист1!F49 = ОКРВВЕРХ.МАТ(B12*1000/D49; 1)
             + ЕСЛИ(ИЛИ(K49="[]";K49="][";K49="[-]"); 0; 1)
```

Полное число линий:

```
промежуточных = ОКРВВЕРХ(H / шаг) − 1
плюс верхний и нижний = 2
ИТОГО линий = ОКРВВЕРХ(H / шаг) + 1
```

`F49` считает то же самое, но **в эквиваленте сечений**: у сдвоенного
сечения верхний и нижний одиночные ригели вместе дают одно сечение,
поэтому поправка `+1` там не добавляется, а у одиночного — добавляется.
Алгебраически модели совпадают ровно:

```
сдвоенное:  (F−1)·шаг·Z + 2·шаг·Y  =  (F−1)·шаг·Z + шаг·Z  =  F·шаг·Z   (т.к. Z = 2Y)
одиночное:  F = ОКРВВЕРХ(H/шаг) + 1 = число линий как есть
```

Проверено численно на 21876 (`[]`, Z = 2Y): модель ранга 102,62 кг против
модели ведомости 102,61 кг на пролёт рам.

### Зависимости

| От чего | Зависит? | Как |
|---|---|---|
| Высота стены | **да** | прямо в `ОКРВВЕРХ(B12/шаг)` |
| Шаг ригелей | **да** | он же знаменатель; подбирается перебором 500…3000 мм с шагом 10 |
| Тип профлиста | **да, косвенно** | марка задаёт макс. шаг `B24`/`B29` по таблице пролётов `B109:B158` |
| Ветровой район | **да, косвенно** | через `C3` → момент → `AD7 ≤ 1` ограничивает шаг сверху |
| Шаг рам | **да, косвенно** | входит в момент (`B13²/8`) и в тяготеющую ширину |
| Утеплённый / холодный | **да** | у утеплённого шаг ограничен списком `D10:F10` = 640 / 1290 |
| Окна, ворота, двери | **НЕТ** | полей проёмов в книге нет вовсе |

### Таблица по высотам

Типовой холодный ангар: профлист С18-1150-0,5, ветровой район II, тип
местности В, γn = 0,8, шаг рам 6 м, высота профиля не зажата.

| H стены, м | Шаг, мм | `F49`, линий | Сечение | Профилей на линию |
|---|---|---|---|---|
| 3 | 1500 | 2 | `[]ПП 145x45x1,2` | 2 |
| 4 | 1500 | 3 | `[]ПП 145x45x1,2` | 2 |
| 5 | 1300 | 4 | `[]ПП 110x45x1,2` | 2 |
| 6 | 1500 | 4 | `[]ПП 145x45x1,2` | 2 |
| 7 | 1500 | 5 | `[]ПП 145x45x1,2` | 2 |
| 8 | 1450 | 7 | `]ПС245х65х1,2` | 1 |
| 9 | 1500 | 7 | `]ПС245х65х1,2` | 1 |

Числа не универсальны: при другом ветре, марке профлиста или зажиме
высоты профиля меняется и шаг, и сечение. Строки 8 и 9 м показывают
переключение на ОДИНОЧНОЕ сечение и работу поправки `+1`.

---

## 4. TASK 3 — одиночный или сдвоенный

**Ответ раздельный по типам ригеля.**

### Промежуточные ригели — по типу сечения

| Сечение | Профилей на линию | Масса сечения `Z` | Узел `AA` | Строк в каталоге |
|---|---|---|---|---|
| `]` | **1** | `Z = Y` (отношение ровно 1) | 0,75 | 122 |
| `[]` | **2** | `Z = 2Y` (ровно 2) | 1,5 | 72 |
| `][` | **2** | `Z = 2Y` (ровно 2) | 0,75 | 50 |
| `[-]` | **2 + полоса** | `Z = 2Y + 1,4211` либо `+2,0527` кг/м | 1,5 | 50 |

Доказательства:

1. **Отношение масс в каталоге.** У `]`, `[]`, `][` отношение
   `Z/Y` целое и постоянное. У `[-]` оно **нецелое** (2,30…2,60), а
   остаток после вычитания двух профилей постоянен — 1,4211 или 2,0527
   кг/м. То есть `[-]` — два профиля плюс полоса.
2. **Формулы объектной ведомости.** 21604, где выбраны как раз `[-]`:
   ```
   ПС 145х45х1,5 = 8*2*12*2                          = 384 п.м.
   ПС 145х45х1,2 = 7*2*6*2 + 7*2*10,7*2 + 5*2*37,3*2 = 1213,6 п.м.
   ```
   Множитель **ровно 2**, полоса в метраж НЕ попадает — она учтена только
   в массе (`1213,6/2 × 6,1823 = 3751` кг против 3 750 в ведомости).
   21876 (`[]`): `5*2*12*2 + 4*2*30*2 = 720` п.м. — тот же множитель 2.

### Верхний и нижний ригели — всегда ОДИНОЧНЫЕ

```excel
'Расчет Угловая'!TO6 = "масса нижнего ригеля"     TO7 = Y7
'Расчет Угловая'!TN6 = "масса верхнего ригеля"    TN7 = TO7
```

Обе ссылаются на `Y` — массу ОДНОГО профиля, тогда как промежуточные
берут `Z` — массу сечения. Это прямое formula evidence: крайние ригели
одиночные независимо от типа сечения.

> Практическое следствие: у сдвоенной стены верхняя и нижняя линии — по
> одному профилю, а не по два. Именно поэтому в `F49` для сдвоенных
> сечений нет поправки `+1`.

---

## 5. TASK 4 — вертикальные стойки ограждения

### Формулы

```excel
'Расчет Угловая'!T4 = ЕСЛИ(Лист1!$B$13<=2; 0;
                       ЕСЛИ(Лист1!$B$13<=4; 1;
                        ЕСЛИ(Лист1!$B$13<=6; 2; 3)))      ← стоек на ПРОЛЁТ РАМ
'Расчет Угловая'!T5 = $T$4 * Лист1!$B$12                   ← суммарная длина, м
'Расчет Угловая'!T7 = ЕСЛИ(R7; 0;
                       $T$5 * ИНДЕКС($C$14:$C$24;
                                     ПОИСКПОЗ(N7;$B$14:$B$24;0);1))  ← масса, кг
```

```
stud_count(на пролёт рам) = 0 | 1 | 2 | 3  по шагу рам (см. T4)
stud_length               = высота стены B12 (каждая стойка на всю высоту)
stud_total_length         = stud_count × B12
stud_mass                 = stud_total_length × вес/м профиля стойки
```

Шаг стоек — следствие, а не вход: правило держит подпролёт около 2 м.

| Шаг рам, м | Стоек | Подпролёт, м |
|---|---|---|
| 2 | 0 | 2,00 |
| 3 | 1 | 1,50 |
| 4 | 1 | 2,00 |
| 5 | 2 | 1,67 |
| 6 | 2 | 2,00 |
| 7,5 | 3 | 1,88 |
| 9 | 3 | 2,25 |

### Профиль стойки — таблица `A13:C24`

Подбирается по **высоте горизонтального ригеля**, чтобы стойка и ригель
легли в одну плоскость:

| Высота ригеля, мм | Профиль стойки | Вес, кг/м |
|---|---|---|
| 105, 110 | `]ПП 110x45x1` | 1,5106 |
| 145 | `]ПП 145x45x1` | 1,7846 |
| 150 | `]ПП 150x45x1` | 1,8237 |
| 170 | `]ПП 170x45x1` | 1,9803 |
| 195 | `]ПП 195x45x1` | 2,1759 |
| 200 | `]ПП 200x45x1` | 2,2151 |
| 220 | `]ПП 220x45x1` | 2,3716 |
| 245, 250 | `]ПП 250x65x1,2` | 3,5004 |
| 300 | `]ПГССигма300х80х1,2` | 4,7534 |

**Стойка всегда ОДИНОЧНАЯ** (`]`) и всегда толщиной 1,0 мм (1,2 мм для
двух крупнейших).

### Критично: в v1.5 стойки ВЫКЛЮЧЕНЫ

```excel
Лист1!V107 = "Без стоек"      Лист1!W107 = ИСТИНА
'Расчет'!R639 = ЛОЖЬ                                   ← у строк «+ стойки»
'Расчет'!S639 = ЕСЛИ(Лист1!$W$107=ЛОЖЬ; ИСТИНА; R639)
JW-условие требует  $S7 = ИСТИНА
```

При `W107 = ИСТИНА` получается `S = R`, а у всех 232 строк каталога с
`раскреп = "да"` (то есть «+ стойки») стоит `R = ЛОЖЬ`. Значит эти строки
**никогда не проходят отбор**, и `T7` у выбранных строк всегда 0.

Строки «+ стойки» (`V639:V646` и далее) — это тот же одиночный профиль,
но с повышенной несущей способностью: `W639 = 0,68/0,77*P4` против
`W7 = 0,2/0,86*P4` у неподкреплённого `]ПП 110x45x1`. То есть стойка
раскрепляет ригель и позволяет больший шаг.

Чтобы включить механизм, достаточно поставить `W107 = ЛОЖЬ`.

---

## 6. TASK 5 — отношение к основной раме

**Ответ: A — самостоятельный элемент ограждайки.**

- Стойка ограждения (`T`, таблица `A13:C24`) живёт внутри калькулятора
  ограждайки, подбирается по высоте ригеля и считается на пролёт рам.
- Колонна основной рамы — в другом файле (подборщик сечений), профили
  ПГС-сигма, в ведомости строка «к».
- Стойка фахверка — третья сущность: в калькуляторе ограждайки её нет
  вовсе. В SprintM она реализована отдельно (`facadePost`, правило «4
  стойки на здание при пролёте ≤18, иначе 6»), и источник этого правила —
  устное указание, а не формула.

Объединять их нельзя: разные профили, разные правила количества, разные
источники.

---

## 7. TASK 6 — продольная стена и торец

Расчётные листы «Расчет Угловая» и «Расчет Рядовая» **идентичны по
структуре**: те же 864 строки каталога, та же матрица, те же формулы.
Разница только во входах и в одном коэффициенте.

| Что | Продольная | Торцевая | Откуда |
|---|---|---|---|
| Длина стены `B11` | длина здания | пролёт | вход |
| Высота стены `B12` | до карниза | до конька | вход |
| Шаг стоек `B13` | шаг рам | **свой**, шаг стоек торца | вход |
| Пиковый аэродин. к-т | зона задаётся отдельно | то же | `F6 = 2,2` угловая, `G6 = 1,4` рядовая |
| Профиль, шаг, ряды | считаются одинаково | одинаково | одна матрица |

Подтверждено сохранёнными расчётами: у 21876 шаг стоек торца 4 м против
6 м на продольной, у 21604 — 6 м против 5,35 м.

Зоны считаются в обеих стенах одинаково:

```excel
e = МИН(Лист1!B7; 2 × MAX(5; Лист1!B8))
ширина угловой зоны = e/5
'Расчет Угловая'!C8 = 2 × ЕСЛИ(ширина/B13 < 0,5; 0; ОКРВВЕРХ(ширина/B13;1)×B13)
'Расчет Рядовая'!C8 = Лист1!B11 − 'Расчет Угловая'!C8
```

Зависимость от пролёта здания — только через `e` (в `B7` идёт длина
здания, а `B8` — высота торцевой стены).

---

## 8. TASK 7 — проёмы

**В калькуляторе ограждайки полей проёмов НЕТ.** Поиск по всем листам на
`обрамлен`, `ворот`, `двер`, `окн` не дал ни одного попадания во входном
блоке `Лист1!B2:B40`.

В объектной ведомости прогоны считаются по ЦЕЛЫМ стенам:

```
21876: C35 = 5*2*12*2 + 4*2*30*2        ← ни одного вычета на проёмы
```

| Тип проёма | Стойки | Ригели | Вычет прогонов | Обрамление |
|---|---|---|---|---|
| Ворота | не в этой книге | не в этой книге | **нет** | в подборщике, `Лист1!O23:O24` |
| Двери | — | — | **нет** | `Лист1!O25` |
| Окна | — | — | **нет** | `Лист1!O26` |
| Ленточное остекление | — | — | **нет** | `Лист1!O27` |

Обрамление проёмов живёт в ДРУГОЙ книге — «Таблица по подбору сечений …
версия 1,5», `Лист1!O23:O29`. Для окон:

```excel
O26 = (E24*B7 + E37*(2*B6 + B7)) * кол-во
```
где `E24`/`E37` — погонные веса нижнего и верхнего ригелей окна, `B7` —
шаг рам, `B6` — высота окна: нижний ригель на пролёт шага рам, верхний —
на две стойки по высоте окна плюс перемычку. Надбавки 1,05 в этой строке
нет, в отличие от строк по воротам и дверям.

Вычет из площади ОБШИВКИ при этом есть и работает — он в ведомости, а не
в калькуляторе прогонов.

---

## 9. TASK 8 — холодный против утеплённого

| Элемент | Холодный | Утеплённый | Разница |
|---|---|---|---|
| Семейства профилей | ПП, ПС | ТПП, ТПС, ТПГС | **в v1.5 утеплённые ВЫКЛЮЧЕНЫ** белым списком `Лист1!V89:W94` |
| Требуемое утепление `U` | 0 | 100 / 150 / 200 / 250 | `Лист1!J89:L93` по типу покрытия |
| Шаг ригелей | любой 500…3000 | **только `D10:F10`** = 640, 1290 | `ЕСЛИ(B10="нет";ИСТИНА;ИЛИ(шаг=D10;E10;F10))` |
| Флаг `B10` | «нет» | «да» | `= ЕСЛИ(Лист1!B18="профлист";"нет";"да")` |
| Горизонтальные ригели | есть | есть | одинаково по структуре |
| Стойки | выключены | выключены | одинаково |
| Кронштейны | `AA` = 1,5 / 0,75 | то же | одинаково |
| Наружный лист | профлист С-18 | профлист или панель | в ведомости, не здесь |
| Внутренний лист, мембраны, утеплитель, крепёж | отсутствуют | **в книге отсутствуют** | ограждайка их не считает |

Вывод: утеплённый вариант в v1.5 подбираться **не может** — и по
семействам, и потому что доступны лишь два фиксированных шага.

---

## 10. TASK 9 — сверка масс

| Объект | Позиция | Расчёт | Ведомость | Δ |
|---|---|---|---|---|
| 21876 | `[]ПП 145x45x1,2` метраж | 720 п.м. | 720 | **0** |
| 21876 | масса профиля | 1539,2 кг | 1540,8 | 0,1 % (округление 2,14 против 2,1378) |
| 21876 | кронштейны | 90 кг | 90 | **0** |
| 22258 | метраж 1,2 + 1,5 | 644 + 200 п.м. | 644 + 200 | **0** |
| 22258 | кронштейны | 117 кг | `(36+15+7,5)*2` = 117 | **0**, сходятся все три слагаемых |
| 21604 | `[-]ПС145х45х1,5` | 384 п.м. | 384 | **0** |
| 21604 | `[-]ПС145х45х1,2` | 1213,6 п.м. | 1213,6 | **0** |
| 21604 | масса `[-]` 1,2 | 3751,4 кг | 3750 | 0,04 % (округление) |

Формула, дающая паритет:

```
метраж  = ряды × длина_зоны × число_стен × профилей_на_линию
масса   = ряды × длина_зоны × число_стен × масса_СЕЧЕНИЯ
кроншт. = узлов × AA,  узлов(угл) = ряды × длина/шаг,
                       узлов(ряд) = ОКРВВЕРХ(ряды × ОКРУГЛ(длина/шаг;1))
```

**Расхождений не осталось.** Полоса сечения `[-]` входит в массу и не
входит в метраж — воспроизведено как есть.

Два расхождения В САМИХ ИСХОДНИКАХ, не в расчёте:

1. **21876, кронштейны.** Ведомость (90 кг) собрана при шаге стоек торца
   6 м, а сохранённая копия калькулятора по тому же объекту — при 4 м
   (105 кг). Метраж при этом одинаков.
2. **Масса узловой сборки.** Лист `несушки!U` даёт 4,8 / 2,4 кг, а
   расчётный лист `AA` — 1,5 / 0,75 кг (ровно в 3,2 раза меньше). В
   расчёте и в ведомости используется `AA`; `несушки!U` не участвует.

---

## 11. Ячейки-источники

| Что | Ячейка |
|---|---|
| Высота расчётной стены | `Лист1!B12` |
| Длина расчётной стены | `Лист1!B11` |
| Шаг стоек (рам) | `Лист1!B13` |
| Границы шага зоны | `Лист1!B23:B24` (угл.), `B28:B29` (ряд.) |
| Зажим высоты профиля | `Лист1!B33` / `B34` |
| Флаг «Без стоек» | `Лист1!V107` / `W107` |
| Шаги послойки | `'Расчет'!D10:F10` = 640 / 1290 / 0 |
| Промежуточные ригели | `'Расчет'!TQ$3`, `TQ$4` |
| Верхний / нижний ригель | `'Расчет'!TN6`, `TO6`; `TN7 = TO7 = Y7` |
| Вес стойки | `'Расчет'!T4`, `T5`, `T6`, `T7` |
| Таблица профилей стоек | `'Расчет'!A13:C24` |
| Узловая сборка | `'Расчет'!AA` |
| Масса профиля / сечения | `'Расчет'!Y` / `Z` |
| Каталог-источник | лист `несушки`, столбцы `H:U` |
| Ряды в ведомость | `Лист1!F49` / `F50` |
| Кронштейны в ведомость | `Лист1!G49` / `G50`, масса `H49` / `H50` |
| Масса профиля в ведомость | `Лист1!I49` / `I50` |

---

## 11а. Что добавила монтажная инструкция

Раздел 8 описывает стеновое ограждение как «каркасно-обшивную
конструкцию послойной сборки» и даёт независимое подтверждение тому, что
до этого выводилось только из формул.

**Три типа ригеля различаются способом крепления — и это ровно то, что
видно в Excel:**

| Ригель | Куда крепится (инструкция) | Что в Excel |
|---|---|---|
| Нижний | **к фундаменту** на дюбель-анкеры с шагом 300 мм в шахматном порядке, на два слоя рубероида | `TO` = масса ОДНОГО профиля |
| Промежуточные | **к опорным кронштейнам**, которые крепятся к колоннам рам саморезами по 4 шт. на элемент | `TQ$4*Z` — масса СЕЧЕНИЯ, и `TQ$3*AA` кронштейнов |
| Верхний | **к П-образному элементу карнизной фасонки** по продольным сторонам | `TN` = масса ОДНОГО профиля |

Дословно: «К ним … производится крепление направляющих профилей
**(кроме нижнего и верхнего)**». Это прямо доказывает, почему число
кронштейнов равно `TQ3` — числу ПРОМЕЖУТОЧНЫХ ригелей, а не общему числу
линий: крайние ригели кронштейнов не требуют. Раньше это следовало только
из структуры формулы.

Заодно объясняется, почему крайние ригели одиночные: нижний лежит на
фундаментной ленте, верхний заводится в П-образный элемент фасонки —
в обоих случаях коробчатое сечение конструктивно не нужно.

**«Коробка» = `[]`.** Рисунок 23 даёт три типа соединения направляющего и
стоечного профилей: а) «коробка», б) вставка с вырезом, в) вставка. Это
те же обозначения, что в столбце `J` каталога: `[]` — коробка из двух
профилей, `]` — одиночный, `][` и `[-]` — варианты вставки. Обозначение
описывает УЗЕЛ соединения ригеля со стойкой, а число профилей — его
следствие; отношение масс в каталоге это подтверждает численно.

**Стойки: «расположение таких профилей указано в проекте».** Инструкция
прямо говорит, что раскладка стоечных профилей — проектная, а не
выводимая. Это объясняет, почему в v1.5 механизм выключен, а лестница
`T4` (0/1/2/3 по шагу рам) — оценка по умолчанию, а не правило.

**Обрамление проёмов — часть основного каркаса, а не ограждения.**
Раздел 9: «Для установки ворот любого типа следует предварительно **на
этапе сборки основного каркаса здания** смонтировать дополнительные
стойки и балки». Это подтверждает вывод раздела 8 отчёта: проёмы не
влияют на стеновые прогоны, их обрамление живёт в другой книге.

**Фахверк — отдельная сущность.** На общем виде ангара перечислены по
отдельности «Каркас стенового ограждения» и «Фахверк», а «Стойка
фахверка» показана в составе ТОРЦЕВОЙ рамы (рисунок 1) и ставится вместе
с торцевыми рамами, до монтажа ограждения. Подтверждает ответ **A** в
разделе 6: стойка ограждения, стойка фахверка и колонна рамы — три
разные сущности.

**Крепление обшивки.** Профлист крепится к направляющим саморезами с
уплотнительной прокладкой **через волну** (кровельный — в каждую волну,
раздел 7). В SprintM крепёж профлиста пока не считается вовсе.

## 12. Неизвестные зависимости

1. **Вариант добавки +1,3 м** к высоте стены (в тетради расчётчика стоит
   в скобках рядом с +0,5). Когда применяется — неизвестно.
2. **Одиночное сечение `]` не проверено на объекте.** Поправка `+1` в
   `F49` выведена алгебраически и срабатывает в расчёте при высоте стены
   8–9 м, но ни один из трёх объектов на `]` не вышел.
3. **Стойки ограждения не проверены ни на одном объекте** — механизм в
   v1.5 выключен, объектных данных нет.
4. **Шаг стоек торца** расчётчик задаёт руками; правило «пролёт /
   (стоек на торец + 1)» сходится на 21604 и 21876, но не на 22317.
5. **Цена кронштейна** в SprintM отсутствует.
6. **Расхождение `несушки!U` и `AA`** в 3,2 раза не объяснено.
7. **Полоса сечения `[-]`** (1,4211 / 2,0527 кг/м) нигде не заказывается
   отдельной строкой — неясно, входит ли она в поставку профиля.
8. **МЕТОДИЧЕСКОЕ ПРАВИЛО, нарушение которого дважды дало неверный
   вывод.** Ведомость — это книга из пяти листов `12м, 15, 18, 21, 1ск`.
   Живой только **ActiveSheet**; остальные четыре — остатки от проекта,
   с которого шаблон копировали, и их формулы к текущему объекту
   отношения не имеют. Лист нельзя выбирать по имени, похожему на
   пролёт: у 22316 (18×30) активен лист `12м`, у 21777 (12×36) — лист
   `18`. Всегда брать `$wb.ActiveSheet` и сверять `C8/C9/C10` с папкой.
9. **Под сэндвичем стеновые прогоны в поставку НЕ идут.** Проверено на
   шести книгах с `C12 = "СП"`, все по активному листу:

   | Объект | Размеры | стр. 34–37 |
   |---|---|---|
   | 22316 Березовский | 18×30×5 | `ПС 245х65х1,5` пусто, три `= 0` |
   | 22318 Сургут | 15×24×5 | то же |
   | 22326 Увильды | 10,4×25,7×4 | то же |
   | 22329 Увильды | 12×26×4 | то же |
   | 22285 Коркино | 18×48×6 | то же |
   | 22333 спринт | 20×50×9 | то же |
   | 21777 | 12×36×4,5 односкат | `ПС 145х45х1,2` = 0, `ПП 145х45х1,5` = 0 |
   | 22251 | 5×12×3 односкат | то же |

   Кронштейнов в сэндвич-ведомостях нет вовсе: на шести книгах строка 38
   занята внутренним уголком, на 21777 и 22251 «Кронштейны» = 0.
   Это подтверждает правило постановки задачи и текущее поведение кода.
10. **`ПС 145х1,5 = 4 × длина` — водосток.** `22318` (лист 15)
   называет позицию дословно «ПС 145х1,5 водосток»; на активных листах
   маркер столбца `A` — «вод». Односкатные книги дают `2 × длина`
   (21777, 22251) — один карниз вместо двух, что подтверждает чтение.
   **Обнуление под профлистом — решение по объекту, а не правило:**
   21876 и 22258 зануляют (`4*C9*0`), а 22304 (18×40×7,5, профлист)
   считает 160 п.м. на 72 072 ₽. SprintM зануляет всегда и на 22304
   разойдётся. Вопрос 1.
11. **Строка 35 «сс» на неактивных листах** — заготовка формулы стоек
   (`периметр / шаг рам × высота × 2 × 1,1`). На активных листах ни
   одного объекта она не задействована, поэтому по-прежнему **нет ни
   одного объектного подтверждения стоек ограждения**.

---

## 13. Предлагаемая модель Core2

Только предложение; production code не писался.

```
WallHorizontalGuide
  kind            : "top" | "intermediate" | "bottom"
  profile         : WallPurlinProfile           ← строка каталога V:AA
  multiplicity    : 1 при kind ∈ {top, bottom}
                    иначе по sectionType: "]"→1, "[]"/"]["/"[-]"→2
  massPerMetre    : kind ∈ {top,bottom} ? profile.massProfile : profile.massSection
  spacing_mm      : подобранный шаг
  countPerBay     : kind = intermediate ? ОКРВВЕРХ(H/шаг)−1 : 1
  length_m        : шаг рам
  source          : 'Расчет'!TQ$4*Z | TN*B13 | TO*B13

WallStud                       ← в v1.5 не создаётся (W107 = ИСТИНА)
  profile         : из таблицы 'Расчет'!A13:C24 по высоте ригеля
  multiplicity    : 1
  countPerBay     : 0|1|2|3 по шагу рам ('Расчет'!T4)
  length_m        : высота стены
  massPerMetre    : из C14:C24
  enabled         : Лист1!W107 = ЛОЖЬ
  source          : 'Расчет'!T7

Bracket
  countPerBay     : ОКРВВЕРХ(H/шаг)−1
  massEach_kg     : profile.jointMass (AA): 1,5 для []/[-], иначе 0,75
  source          : 'Расчет'!AA, Лист1!G49/H49

EndWallStud       ← не сущность этой книги; см. facadePost
OpeningJamb       ← не сущность этой книги; см. 'Таблица по подбору сечений'!O23:O27
OpeningHeader     ← то же, O26
```

---

## 14. Ответы на критические вопросы

**A. Стеновой горизонтальный прогон — одиночный или сдвоенный?**
**Зависит от типа сечения выбранной строки каталога, и по-разному для
крайних и промежуточных.** Промежуточные: `]` — одиночный, `[]` и `][` —
сдвоенный, `[-]` — сдвоенный плюс полоса. **Верхний и нижний —
ВСЕГДА одиночные** (`TN7 = TO7 = Y7`, масса одного профиля).

**B. Стоечный профиль — одиночный или сдвоенный?**
**Одиночный**, всегда `]`, толщина 1,0 мм (1,2 мм для 250 и 300).

**C. Сколько горизонтальных рядов при высоте 3…9 м?**
Однозначного числа нет — зависит от подобранного шага. Формула:
`ряды = ОКРВВЕРХ(H/шаг) + (сечение одиночное ? 1 : 0)`. Для типового
холодного ангара (см. таблицу в разделе 3): 3 м → 2, 4 м → 3, 5 м → 4,
6 м → 4, 7 м → 5, 8 м → 7, 9 м → 7.

**D. Сколько вертикальных стоек на длинной стене и на торце?**
**В v1.5 — ноль на обеих**: механизм выключен `W107 = ИСТИНА`. Если
включить: `стоек = T4(шаг рам) × число пролётов рам в стене`, одинаково
для обеих стен, но у торца свой шаг стоек.

**E. Какой их шаг?**
Прямо не задаётся. `T4` держит подпролёт около 2 м: 0 стоек при шаге рам
≤ 2 м, 1 при ≤ 4, 2 при ≤ 6, 3 свыше.

**F. Меняется ли количество при другом frame_step?**
**Да, у обоих элементов.** У стоек — прямо через `T4`. У ригелей —
косвенно: шаг рам входит в изгибающий момент (`B13²/8`) и в тяготеющую
ширину, поэтому меняет допустимый шаг ригелей, а через него и число рядов.

**G. Как влияют ворота, двери и окна?**
**На стеновые прогоны — никак.** Полей проёмов в книге нет, ведомость
считает целые стены. Вычет идёт только из площади обшивки, а обрамление
считается в другой книге (`O23:O27`).

**H. Exact wall-frame BOM formula**

```
ряды          F = ОКРВВЕРХ(H_стены / шаг) + (сечение ∈ {]} ? 1 : 0)
метраж профиля  = F × длина_зоны × число_стен × (сечение ∈ {]} ? 1 : 2)
масса профиля   = F × длина_зоны × число_стен × Z
узлов (угловая) = F × длина_зоны / шаг_стоек
узлов (рядовая) = ОКРВВЕРХ(F × ОКРУГЛ(длина_зоны / шаг_стоек; 1))
масса узлов     = узлов × AA
масса стоек     = 0            (v1.5, W107 = ИСТИНА)
```

---

## 15. Финальные статусы

| Статус | Значение | Обоснование |
|---|---|---|
| `HORIZONTAL_GUIDE_SEMANTICS_PROVEN` | **YES** | подписи `TN6`/`TO6` и формулы `TN7 = TO7 = Y7` против `TQ$4*Z`; независимо подтверждено разделом 8 инструкции — нижний крепится к фундаменту, верхний к карнизной фасонке, промежуточные к кронштейнам |
| `HORIZONTAL_GUIDE_MULTIPLICITY_PROVEN` | **YES** | отношения масс в каталоге + формулы ведомостей 21876 и 21604 (множитель 2), крайние — по `Y` |
| `WALL_STUD_SEMANTICS_PROVEN` | **YES** | `T4`/`T5`/`T7` и таблица `A13:C24`; инструкция подтверждает конструкцию и типы узлов (рис. 23) |
| `WALL_STUD_COUNT_PROVEN` | **NO** | инструкция прямо говорит: «расположение таких профилей указано в проекте». Лестница `T4` — оценка по умолчанию, а не правило; объектных данных нет |
| `WALL_FRAME_BOM_PARITY_PROVEN` | **YES для прогонов и кронштейнов / NO для стоек и обрамления** | метраж и массы сошлись в ноль на 21876, 22258, 21604; стойки и обрамление не проверялись |
| `SAFE_TO_IMPLEMENT_CORE2_WALL_FRAME` | **YES для горизонтальных ригелей и кронштейнов; NO для стоек** | у стоек нет ни одного объектного подтверждения, и в v1.5 они не выбираются |

**PRODUCTION CODE CHANGED = NO**

Изменений в `src/` нет, книги не менялись (SHA-256 совпал до и после),
коммитов и пушей не делалось.
