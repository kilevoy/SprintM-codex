# Передача SprintM в работу Claude

Дата передачи: 2026-09-18
Базовая ветка: `codex/development`
Ветка продолжения для Claude: `claude/wall-envelope-auto-selection`
Рабочий репозиторий: `kilevoy/SprintM-codex`

## Цель

Довести расчёт холодного ангара с профнастилом до подтверждённого Excel-паритета:
автоматически подобрать стеновые прогоны по угловой и рядовой зонам, посчитать
кронштейны, крепёж, массы и стоимость, а затем включить результат в ведомость.

## Что уже сделано

- Добавлен снимок 864 строк каталога из `Калькулятор ограждайки v1.5.xlsx`:
  `src/data/wallEnvelopeProfiles.generated.json`.
- Добавлен воспроизводимый extractor:
  `scripts/extract_wall_envelope_profiles.py`.
- Добавлено изолированное ядро:
  `src/calc/wallEnvelope/types.ts` и `wallEnvelope.ts`.
- `computeProject` принимает необязательный блок `wallEnvelope` и возвращает
  provisional-результат для продольных стен и торцов (профиль передаётся
  явно, автоподбора в `computeProject` пока нет).
- Торцевые стены используют высоту по коньку, продольные — высоту по карнизу.
- Снимок содержит массу `Y:AA`, несущую характеристику `X` и признаки `Q/S`.
- Локальный Excel-прайс не добавлен в Git.
- **(2026-09-18, сессия claude/wall-envelope-auto-selection)** Полностью
  разобрана и воспроизведена матрица `AWX:BGN`/`JW`/`TQ`/`BGQ:BGR:BGS:BGT:BGU`
  на «Расчет Угловая»/«Расчет Рядовая» — см. переписанный
  `docs/parity/wall-envelope-engine-extraction.md`. Реализована и
  Excel-сверенная (до 6 знака) чистая функция автоподбора:
  `src/calc/wallEnvelope/selectWallEnvelopeProfile.ts` +
  `selectWallEnvelopeProfile.test.ts`. Найдена и исправлена ключевая ошибка
  первого черновика: аэродинамический коэффициент ветра — ступенчатая
  функция (`MATCH(...,1)`), а не линейная интерполяция; также обязательна
  проверка `jointFactor_Q`/`jointFactor_S` каталога (в v1.5 все профили
  «+ стойки» всегда невыбираемы).
- **(2026-09-18, продолжение)** Автоподбор доведён до сквозного:
  - `zoneWindPressure.ts` — коэффициент ветровой нагрузки зоны C3
    (w0 × k(ze) × (1+ζ(ze)) × пиковый аэродинамический × γf × γn), обе
    таблицы СП 20.13330; ν в C3 не входит («Пиковая нагрузка без v»);
  - `scripts/extract_wall_decking_spans.py` →
    `src/data/wallDeckingSpans.generated.json` — таблица допустимых
    пролётов обшивки, из которой берутся `Лист1!B24`/`B29`;
  - `autoWallEnvelope.ts` — геометрия зон (e = MIN(b; 2h), e/5, вверх до
    шага рам, с двух сторон), максимальный шаг, подбор и объёмы
    (ряды, кронштейны, массы) — полный аналог `Лист1!B49:I50`;
  - `excelNumerics.ts` — Excel округляет до 15 значащих цифр перед
    ОКРВВЕРХ, из-за чего `4,2/1,4` у него ровно 3; без этого подбор
    уходил на соседний профиль.
  - **Живой оракул**: `scripts/oracle/run_wall_envelope_calculator.ps1`
    гоняет сценарии через КОПИЮ книги (оригинал не открывается на запись,
    SHA-256 сверяется до и после), `compare_wall_envelope_calculator.mjs`
    сравнивает построчно. **10 независимых сценариев × 22 величины
    совпали с Excel полностью**; проверка встроена в `npm run verify`
    (`check:wall-envelope-parity`).
  Функция **ещё не подключена** к `computeProject`/ведомости.

## Что пока нельзя считать готовым

- Автоматический подбор профиля по ветровой таблице Excel ещё не перенесён.
- Формулы `AD4`, `JW`, `TQ`, `BGQ:BGR:BGS:BGT:BGU` не подтверждены построчным
  паритетом в TypeScript.
- Стеновые прогоны пока не входят автоматически в коммерческий итог.
- `wallEnvelope.status = "provisional"` обязателен до завершения проверки.
- Не подгонять ожидаемые значения под SprintM.

## Основной источник истины

Калькулятор:
`Y:\Предварительные расчеты\Калькуляторы\Калькулятор ограждайки v1.5.xlsx`

Эталонный объект:
`Y:\Предварительные расчеты\Предрасчеты на проверку\Каргалейка\21876.xlsx`

SHA-256 базового калькулятора:
`4A9343A1E3149954DEC0F91D5398528F18016A8423EC204CE2B92A59F612DEAF`

Проверочные сценарии: 21876, 22258, 21604 и минимум один дополнительный объект
с профнастилом. Книги открывать через Excel COM только для чтения, выполнять
`CalculateFullRebuild`, закрывать без сохранения.

## План следующего этапа

1. Извлечь из `Расчет Угловая` и `Расчет Рядовая` все колонки, участвующие в
   условии `JW`, ранжировании `TQ` и матрице результата `AWX:BGN`.
2. Извлечь таблицу коэффициентов ветра `AD4` и зафиксировать её связь с
   существующими климатическими справочниками SprintM.
3. Реализовать чистую функцию выбора профиля:
   входы — тип зоны, длина стены, высота, шаг рам, тип местности, ветровое
   давление, материал, утеплитель, границы шага;
   выход — профиль, шаг, коэффициент использования, масса и причина выбора.
4. Сверить вручную и тестами формулы:
   - `AD6`, `AD7`;
   - `JW`;
   - `TQ`;
   - `F49:I50` на `Лист1`.
5. Добавить отдельные строки для стеновых прогонов, кронштейнов, уголков и
   крепежа в ведомость. Стоимость окон, ворот и дверей не включать в
   сопоставимую стоимость.
6. Проверить проёмы: вычеты из площадей, влияние на ряды, оконные ригели и
   обрамление.
7. Проверить режимы поставки:
   - полный комплект с профнастилом;
   - только каркас под профнастил — стеновые прогоны нужны;
   - только каркас под сэндвич-панели — стеновые прогоны исключать только по
     составу поставки;
   - кровельные прогоны учитывать отдельно.
8. После каждого изменения запускать:

```powershell
npm.cmd run verify
```

9. До подтверждения трёх независимых книг оставлять статус `provisional`.

## Git-правила

- Работать только в `codex/development`.
- Для продолжения создать/использовать только `claude/wall-envelope-auto-selection`.
- Не вносить изменения непосредственно в `codex/development` после передачи.
- Не менять `claude/start-session-l3r43f`.
- Не добавлять `.xlsx`, `.xls`, `.env`, секреты и временные файлы.
- Не делать rebase, merge или удаление веток.
- Перед каждым коммитом проверять `git diff --check` и `git status`.

## Критерий готовности

Минимум три независимые книги дают подтверждённый построчный Excel-паритет по
профилю, шагу, количеству рядов, длине, кронштейнам, массе и стоимости без
окон, ворот и дверей. Только после этого можно убрать provisional и включить
автоподбор в пользовательскую форму.
